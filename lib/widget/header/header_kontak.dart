import 'package:flutter/material.dart';

class HeaderKontak extends StatelessWidget {
  const HeaderKontak({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Center(
        child: Text('Registration'),
      ),
    );
  }
}